import React from 'react';
//Adding Forn

import useStyles from './styles';
const Form = () => {
       const classes = useStyles();
    return (
        // Using the classes from styles.js
        <div>
            <h2>Form Component</h2>
            {/* Form elements will go here */}
        </div>
    );
}

export default Form;